# Reverse-proxy Nginx (front public -> conteneur app)
# À déployer sur le VPS / hôte de production.

upstream afriland_app {
  server app:8080;
  keepalive 32;
}

server {
  listen 80;
  server_name _;

  # Redirige tout vers HTTPS en prod
  # return 301 https://$host$request_uri;

  location / {
    proxy_pass http://afriland_app;
    proxy_http_version 1.1;
    proxy_set_header Host              $host;
    proxy_set_header X-Real-IP         $remote_addr;
    proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_connect_timeout 30s;
    proxy_read_timeout    60s;
  }
}

# server {
#   listen 443 ssl http2;
#   server_name biblio.afrilandfirstbank.com;
#
#   ssl_certificate     /etc/letsencrypt/live/biblio.afrilandfirstbank.com/fullchain.pem;
#   ssl_certificate_key /etc/letsencrypt/live/biblio.afrilandfirstbank.com/privkey.pem;
#   ssl_protocols       TLSv1.2 TLSv1.3;
#   ssl_ciphers         HIGH:!aNULL:!MD5;
#
#   location / { proxy_pass http://afriland_app; }
# }
